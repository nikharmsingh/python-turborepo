[project]
name = "code-runner"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastmcp>=2.0",
    "core",
]

[tool.uv.sources]
core = { workspace = true }

[tool.uv]
package = false
