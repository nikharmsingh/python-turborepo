[project]
name = "knowledge-base"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastmcp>=2.0",
    "core",
    "tools",
]

[tool.uv.sources]
core  = { workspace = true }
tools = { workspace = true }

[tool.uv]
package = false
